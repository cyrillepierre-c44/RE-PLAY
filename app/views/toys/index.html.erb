<h1>Toys#index</h1>
<p>Find me in app/views/toys/index.html.erb</p>
<div class=container >
  <ul>
    <% @toys.each do |toy| %>
      <%= link_to "Jouet numéro #{toy.id}, nom: #{toy.barcode}", toy_path(toy) %>
    <% end %>
  </ul>
</div>
